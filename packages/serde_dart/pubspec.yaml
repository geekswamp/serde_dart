name: serde_dart
description: "A Dart library for serializing and deserializing data structures."
version: 0.0.1
homepage:
topics: ["serde", "serde-dart", "codegen", "json", "serialization", "deserialization"]

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  source_gen: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
